# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    
    - script: |
        docker build -t repository.jfd-prod.x100platforma.com/finance/x100group.jetti-server:$(tag) .
      displayName: 'Docker build'

    - task: ArtifactoryDocker@1
      inputs:
        command: 'push'
        artifactoryService: 'artifactory-x100'
        targetRepo: 'docker-local'
        imageName: 'repository.jfd-prod.x100platforma.com/finance.x100group.jetti-server:$(tag)'
        collectBuildInfo: true
        buildName: '$(Build.DefinitionName)'
        buildNumber: '$(Build.BuildNumber)'
