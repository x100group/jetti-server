# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    
    # - script: |
    #     docker build -t repository.jfd-prod.x100platforma.com/x100group-jetti-server:$(tag) .
    #   displayName: 'Docker build'

    - task: Docker@2
      inputs:
        containerRegistry: 'artifactory'
        repository: 'finance'
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
    # - task: ArtifactoryDocker@1
    #   inputs:
    #     command: 'push'
    #     artifactoryService: 'artifactory-x100'
    #     targetRepo: 'docker-localv1'
    #     imageName: 'repository.jfd-prod.x100platforma.com/x100group-jetti-server:$(tag)'
    #     collectBuildInfo: true
    #     buildName: '$(Build.DefinitionName)'
    #     buildNumber: '$(Build.BuildNumber)'
